const renderLogin = (req, res) => {
    console.log("Rendering login page...");
};

const loginUser = (req, res) => {
    console.log("Loggin in user to the API...");
};

module.exports = {
    renderLogin,
    loginUser,
};
